import { InnerLastNamePipe } from './inner-last-name.pipe';

describe('InnerLastNamePipe', () => {
  it('create an instance', () => {
    const pipe = new InnerLastNamePipe();
    expect(pipe).toBeTruthy();
  });
});
