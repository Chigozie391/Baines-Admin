import { LoanPurposePipePipe } from './loan-purpose-pipe.pipe';

describe('LoanPurposePipePipe', () => {
  it('create an instance', () => {
    const pipe = new LoanPurposePipePipe();
    expect(pipe).toBeTruthy();
  });
});
